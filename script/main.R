crawl_script <- glue::glue("{here::here()}/script/namu_crawl.R")
preprocess_script <- glue::glue("{here::here()}/script/namu_preprocess.R")

# source(crawl_script, encoding = "utf-8")
source(preprocess_script, encoding = "utf-8")
source(preprocess_script, encoding = "utf-8")

# source(preprocess_script)
